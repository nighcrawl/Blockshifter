## Agent skills

### Issue tracker

Issues suivies sur GitHub via `gh` CLI — repo à configurer (pas encore de remote git). See `docs/agents/issue-tracker.md`.

### Domain docs

Layout single-context — CONTEXT.md + docs/adr/ à la racine. See `docs/agents/domain.md`.
